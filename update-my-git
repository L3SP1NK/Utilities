#!/bin/bash

CWD=$(pwd)
REPO_DIR=/opt/utilities

cd ${REPO_DIR}
git pull
git add -v * | ccze -A
git commit
git push -v | ccze -A

ln -sf * /bin
cd dot-files
ln -sf .zshrc ~

cd ${CWD}

echo -e "\e[32mDone!\e[0m"
