#!/bin/bash
clear

if [[ ${1} == "-1"  ]]
	then
		curl -sf 'http://wttr.in/Dijon' | grep -v "Follow"
elif [[ ${1} == "-2" ]]
	then
		curl -sf 'http://wttr.in/Dijon?format=v2' | grep -v "Follow"
	else
		echo -e "\e[33mSpecify a version with -<version> (currently 1 or 2) \e[0m"
fi
