#!/bin/bash

if [[ ${1} ]]
	then
		xrandr --output VGA-1 --brightness ${1} --output LVDS-1 --brightness ${1}
		kill -9 $PPID
	else
		echo -e "\e[33mSpecify a brightness level... (from 0.0 to 1)\e[0m"
fi
