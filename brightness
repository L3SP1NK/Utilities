#!/bin/bash

if [[ ${1} == "--vga" ]]
	then
		xrandr --output VGA-1 --brightness ${2}
elif [[ ${1} == "--hdmi" ]]
	then
		xrandr --output HDMI-A-0 --brightness ${2}
	else
		echo -e "\e[33mSpecify a display...\nbrightness --<vga/hdmi>\e[0m"
fi

/bin/check-error
